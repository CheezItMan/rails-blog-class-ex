<h1>HELLO, WE ARE RAILSING!</h1>
<ul>
  <% @posts.each do |post| %>
  <li><%= post.title %>: <%= post.author_name %>
    (<%= post.created_at %>)
    <% if @favorite == post.id %>
    YOUR FAVORITE
    <% end %>
  </li>
  <% end %>
</ul>
