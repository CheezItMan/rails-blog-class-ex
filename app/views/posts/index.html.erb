<h1>HELLO, WE ARE RAILSING!</h1>
<ul>
  <% @posts.each do |post| %>
  <li><%= post.title %>:
    <% if !post.writer.nil? %>
      <%= post.writer.name %>
    <% end %>
    (<%= post.created_at %>)
  </li>
  <% end %>
</ul>
